# Adaptive pricing optimization and ablation

Six half-row/pruning searches and two square-H4 ablations completed. Frozen
source.tar.gz is the executed v2 source. Downloaded/worker1 holds quadratic
and ablation results; worker2 holds cubic results. Transfer receipt confirms
all34 file hashes and both archive hashes. Both manual ablations used v2
adaptive_factor_pricing.py, square H4 fixture, quadratic family, width4,
body1 ideal,80iterations,batch64,atom cap6500,180seconds. square_full_prune
added --prune only; square_half_noprune added --half-rows only. Both exit0.

The current working audit/tests were subsequently strengthened; use archived
sources to reproduce these particular experiments. All32 remote certificates
from both batches were independently replayed with sparse upper witnesses.
No current research process is live. Both A10 hosts intentionally remain active.
