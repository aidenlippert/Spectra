The earlier `h4_exact.json` used a coefficient replay that omitted the
dagger on the left CAR word. It is retained for provenance only and must not
be used as a measurement. The corrected campaign uses
`dagger(left) * right`, matching the repository verifier.
