# Second experiment wave

Authorized by the user on 13 September 2026. The primary test is fresh discovery of both H10 endpoints at width <= 0.0016 Ha, with all discovery and replay costs included. A fresh run of an existing large SOS generator is a baseline, not a new sparse-discovery breakthrough. Original fixtures are exclusively results/certificate_scaling/active_space_ladder/h*/fixture.json; different orbital/basis fixtures need explicit transformation and error accounting.

Root owns control, aggregation, fresh H10 numerical baseline and new upper run. Agents own matching sparse_dual, upper_states, complement, geometry, tensor_network, continuation, information, theory, audit, and gpu subdirectories. The fresh_lower agent owns its audit notes. Existing files and failed runs are retained. No production verifier gates are changed.

Use existing Lambda A for root H10 CPU discovery; B for the GPU agent's matched sparse-action benchmark. Both are existing shared A10 instances; do not launch or terminate instances. Freeze sources, use unique paths, explicit thread counts, bounded process timeouts, and verify retrieved hashes. Resource-budget refusals are not mathematical impossibility results.

Every numerical proposal needs an exact original-H certificate or a clearly scoped diagnostic label. Compute endpoint ordering and widths with Fraction arithmetic. Upper support and selected basis size differ; charge expansion and contraction costs. A principal-block lower must be no greater than any Rayleigh quotient in that block. Rational square-root enclosures must be checked by squaring. PSD checks must reject both [[0,1],[1,0]] and [[1,2],[2,1]], and validate Hermiticity. A failed PSD shift search must move the lower bound downward. Old wrong-fixture and invalid preflight results are excluded from accepted evidence.

Track all attempts, including unsuccessful default-budget probes. No unsupported completeness, treewidth, global-optimality, formal-verification, or scalability claim. H12 is a held-out follow-up only after a defensible H10 candidate; fixture generation itself is not an accuracy result.
