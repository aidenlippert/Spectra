# Same full family without the dense numerical congruence

The original full spin-completion control reached its 240-second outer
watchdog without exporting a certificate. Its complete full-frame numerical
map contained 12,129,902 nonzeros; construction metadata shows that the
whitening congruence connected previously sparse spin components. The
failure and pre-change discovery source are preserved.

Run one additional complete 492-generator control without whitening. No
generator, constraint, PSD entry, residual term or accuracy gate is removed.
Use Clarabel with the same tolerances, a 150-second solver cap and a
240-second outer watchdog. Charge the entire failed original control and
this additional attempt; this is an explicit follow-up, not a replacement
success under the first budget. Do not change solver or truncate the family
after observing this result.

Export and replay through exactly the same rational certificate boundary.
Add any accepted result to fresh interval replay. A solver failure remains
indeterminate about this family's optimum.
