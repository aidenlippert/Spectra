# Local response construction: bounded investigation

Continue the accepted response mathematics without changing its accuracy claims.
The unresolved target is a directly constructible and checkable retained
positivity / relative-residual proof with controlled cost on interacting systems.

The executable test is the same fully interacting open 2-by-4 Hubbard ladder,
t=1, U=8, four up and four down electrons. Hubbard energies are in t units,
not hartrees. The diagnostic interval target is 0.001 t. A local proof may use
all local charge sectors, but may not fix a fragment's charge. The controlled
representation is overlapping two-rung blocks with arbitrary number-conserving
one-rung boundary operators, followed by enlargement if warranted. Every term
must reconstruct exactly; local positive matrices are checked rationally.

Compare local positivity with and without boundary operators. Separate a valid
lower bound from a sharp enough bound. If a local family fails, seek an exact
dual witness; optimizer failure alone is not a cone limitation.

Global enumerated states are allowed solely as explicitly charged diagnostics
and for obstruction witnesses, not as a claimed compact discovery/checker.
No global state is used to select the local optimization structure. A compact
lower component alone is not a complete enumeration-free interval.

Use one bounded heavy subprocess at a time on the local machine; record failed
and timed-out runs. No paid compute or Git writes. All previous artifacts remain
unchanged. Exact checks, numerical diagnostics, and unproved candidate formulas
must be labeled separately. A local SDP or a repeated known identity is not a
field-level breakthrough.
