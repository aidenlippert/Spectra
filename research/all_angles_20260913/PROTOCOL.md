# Multi-agent all-angle campaign

The user authorized multiple agents and the existing Lambda instances for mathematical and GPU experiments. This bounded campaign screens all 72 listed directions, executes representative mechanisms, and expands promising numerical routes. An untested direction is recorded as untested; a prerequisite check is not a chemistry experiment.

Canonical molecular comparison inputs are `results/certificate_scaling/active_space_ladder/h{4,6,8,10}/fixture.json`. Their raw hashes and sectors bind the comparisons. Prior certificates and FCI upper states may be validation references only unless reuse is explicitly labeled. Total-energy width target is 0.0016 Ha for those finite fixtures. Abstract toy energies have no implied Hartree conversion.

Work is isolated under matching `research/all_angles_20260913/<track>` and `results/all_angles_20260913/<track>`. Agents own their tracks and preserve other files. Mathematical or computational negative results count as evidence only for the tested assumptions/family.

Tracks: Hamiltonian-only upper states; sparse dual discovery; spin-multiplier ablation; Schur/complement bounds; relative perturbation/continuation; information/functional routes; interaction geometry; frontier quantum/prerequisite and exact-oracle checks; actual GPU Ritz solves; actual tensor-network discovery; independent audit. Root additionally owns sparse selected-CI refinement and aggregation.

Use existing host A 129.159.32.200 and host B 146.235.200.232. Live API inventory on this turn confirms each is one A10, 30 vCPUs and advertised 200GiB RAM, $1.29/hour. Existing instances remain under prior shared ownership; no new instance launch or termination is implied. Jobs use unique remote directories, process timeouts, frozen source hashes, and retrieved result hashes. B GPU has a single assigned owner, GPU track. CPU jobs set explicit BLAS thread limits. Runtime snapshots distinguish GPU arithmetic from CPU work on a GPU instance.

Before claiming results: exact or rigorously bounded original-H endpoints; independent replay or a clearly labeled prior receipt; valid sector and normalized nonzero upper witness; costs including candidate generation, state expansion, matrix construction, solve, export, and replay. GPU floating eigenvalues and exact diagonalization used as diagnostics are not certified endpoints by themselves. Record oracle use, full sector enumeration, numerical/drop thresholds, rational precision, and unsuccessful attempts.

The audit explicitly checks source-fixture mismatch, zero-pivot PSD refusal, use of a numerical ground energy as an upper endpoint, false diagonal-complement bounds, false treewidth claims, and the difference between Sz-weight-zero and SU(2)-invariant operators. No production verifier gates are weakened by this campaign.
