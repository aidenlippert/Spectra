# Composable response: remove determinant dependence

Preserve the previous 778-file manifest and the achieved frozen-H6 1.000 mHa
interval. That numerical milestone is complete. The next primary test is a
second operator-level elimination, constructed and checked without generating
the retained determinant space. Byte compression after enumeration does not
count as that result.

Use the same first partition and certified response. Write its retained
operator as K1=A1−B1 F1(D1) B1*, where F1=2p1−D1 p1². The scalar residual
penalty eta1 is carried explicitly. Since 0<=F1(D1)<=D1^-1<=I/delta1,
the lower envelope K1>=A1−B1 B1*/delta1 is available without evaluating
the response on every retained vector. Test a second double-occupancy sector
inside P1, retaining the induced correction and every error shift.

First perform matched H6 controls: the same exact retained-factor machinery
with zero response, constant response, and the existing polynomial response;
also compare a direct enumerated H−b factor to identify what enumeration
alone buys. Invalidly discarding coupling is not a control. Exact negative
expectations may certify failure of a particular sufficient condition.

The main operator test may use canonical CAR words and small spatial matrices;
no full determinant basis, many-body block matrix, or inherited retained
eigenvector is allowed in its construction or accepting checker. Bounds are
selected by verified energy consequences. Keep the first response implicit.
Cap operator expansion at 100,000 canonical words and 60 measured seconds
per molecular case. Any nonpositive lower estimate is an inconclusive gap
certificate, not proof that the true gap is absent. A precise obstruction
must identify the particular envelope/representation it excludes.

The full-interval transfer control uses the existing H6 1.6 A fixture and
targets 1.4 mHa with the same polynomial stopping rule. A newly enumerated
upper or retained proof is permitted as a charged diagnostic, and must not
be called an enumeration-free transfer. H8 receives the same operator rule
under the same word/time caps; no H8 full-sector matrix is authorized here.
Do not tune the original H6 below 1.000 mHa.

Numerical compute budget for the main finite controls is 180 seconds, excluding
the separately capped CH2 pilot and exact verification. Exact rational replay,
focused tests, dependency provenance and failed attempts are part of the
result. If the recursive closure fails, report the mathematical obstruction
and the remaining operator or action-cost dependency.
