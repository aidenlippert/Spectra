# H8 fixed-number completion campaign

The user's pasted request is the task specification. `/Users/aidenlippert/DERIVATION.md`
is supporting mathematics, independently implemented and checked here; its claimed
prototype was not found among the supplied files or the bounded adjacent search.

Preserve the previous H6 achievements, strong H8 proof, and all sealed results.
First diagnose the actual strong H8 sextic contribution and number multiplier,
including its nonzero export residual. Do not use that full proof to train or
select the new operator spans.

The initial new construction retains every operator direction in the preserved
compact H8 `dual48` span. Restore independent charge-compatible cubic weights
and collective number-sector matching. In full coefficient form this is
`S6 + L2(X4)=0`; equivalently `S6=L2(V)`, `X4=-V`, with the contraction-free
remainder constrained to zero. Spin averaging applies to the actual aggregate
positive operator and commutes with the particle-number lift. Ordinary quadratic
spin multipliers and the separate nonsinglet proof remain required.

The previous paired point is a feasible special case, with equal partner Gram
blocks and zero quartic number multiplier. All complete energy claims use the
same frozen rational H8 upper, the full fixed-N lower, and the original-H spin
defect charged once. Numerical solver candidates are not certificates: the
unchanged rational CAR replay, with any explicitly verified residual witness,
is the accepting gate.

Use one heavy local run at a time, record construction, solve, failure, and replay
costs, and bound each process. Polynomial orbital coefficient spaces are allowed;
full N-particle determinant enumeration and invented three-body moments are not.
If direct projection is too costly, measure that limitation before changing its
representation. A finite numerical search is not an exact cone obstruction.

Acceptance target: a complete H8 interval at most 1.6 mHa. Also report the matched
paired comparison, any computational restriction introduced, and all inherited
state/compact-span preparation costs separately from fresh marginal costs.
