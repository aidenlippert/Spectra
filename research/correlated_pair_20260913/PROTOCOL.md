# Compact correlated states and inexpensive two-sided certificates

Continue the accepted global_response_20260913 mechanisms and fixtures.
Preserve every existing file; all new source and results live in this campaign.
The inherited evidence inventory has one pre-existing mismatch: the previous
factor branch's analysis-only JSON was corrected after the prior seal. Record
both hashes and preserve its current contents. Accepted certificates and
numeric results match the prior inventory; do not rewrite its manifest.

First integrated milestone: directly optimized correlated MPS uppers within
0.500 mHa of the accepted H6 and corrected H8 uppers, paired immediately with
the existing strong cubic lower proofs. Exact rational tensor contractions,
norm and particle/spin-sector checks accept the rounded trial itself. Never
use an FCI vector to initialize the main builder. Charge labels, all tensor
entries, active bond dimensions, intermediates and cost remain visible.

Main lower branch: implement a structured self-consistent cubic construction
and test the actual density-conditioned closure proposed in Hastings 2026.
Use exact CAR products. Expectations guide candidates only; every final
factor is positive and every generated term is kept or bounded. Correlated
moments and old SOS proofs may teach the rule on development cases, but a
new-case builder cannot silently import either an FCI vector or a full Gram
solution. Distinguish independent construction from teacher assistance.

Independent complement branch: use the actual correlated state and exact
residual; prove a complement bound or exhibit a precise limitation of a
specified local parent/factor family. Numerical excitation energies and
occupation-sector gaps do not certify that new complement.

Compare direct and response-assisted complete bounds. Preserve the 237/175
action results; do not optimize response degree as this campaign's default.
Use width = reference width + upper deterioration + lower deterioration to
allocate error. H6 1.4, 1.6, 1.73 Angstrom and H8 are known cases. Freeze a
selected rule before any new geometry; choose that geometry only after
checking existing fixture history. CH2 is a bounded model/spin transfer.

Resource accounting is prospective. Every numerical experiment, dependency
installation, exact replay and test run uses budget.py with a unique name and
explicit wall timeout. Failed/time-limited attempts keep their logs and JSON
receipts. Initial per-attempt caps are 300 seconds; initial aggregate compute
cap is 2400 measured process-wall seconds across all branches, checked by
the root before extensions. One BLAS/Numba thread, target peak <=1 GiB per
experiment. Exact verification is charged too. No FCI-sized dense matrix
except separately authorized tiny H4 test oracles. No H10 unless costs justify
it. No publishing, cloud provisioning or purchases.

A local isolated dependency environment is permitted if the existing tensor
code cannot supply the requested optimizer. Keep existing environments intact.
Do not stop at a schema or descriptor: run discovery, independent acceptance,
integration, transfer and rejection tests. Report the strongest construction
and honest unresolved gaps, without a universal scaling claim.
