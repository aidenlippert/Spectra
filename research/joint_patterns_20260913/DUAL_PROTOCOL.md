# Exact diagnostic after the frozen seven-case campaign

The joint ten-pattern H6 solve returned an inaccurate numerical optimum and
an exact lower certificate essentially equal to the quadratic baseline.
One bounded additional diagnostic will attempt a rational feasible dual
witness for precisely this joint cone. It does not change any campaign run,
factor selection, retained Hamiltonian, budget result, or upper witness.

Round the saved dual proposal to denominator 10^12, impose Hermiticity and
every one-body number-ideal equality by exact affine elimination, and mix
with the fixed-N uniform trace functional. The predetermined mixing fractions
are 0, 10^-8, 10^-7, 10^-6, 10^-5, 10^-4, and 10^-3. Accept only after exact
PSD checks of every quadratic block and all four joint pattern blocks,
normalization, the coefficient box, and exact ideal annihilation. The
diagnostic has a separate 120-second outer process budget, including export
and replay. A failure remains inconclusive about cone sufficiency.

A feasible dual objective is a ceiling on this method's retained-model lower
bound. Add the fixed lower tail endpoint for its ceiling on the molecular
lower bound. Comparing that ceiling with the frozen exact upper gives a
minimum achievable width using that upper. This is a bound on the certificate
family, not a lower bound on the physical energy error. A physical error
obstruction requires a separately proved physical lower bound. Do not confuse
the two statements or imply that other pattern-based constraints are excluded.

After an accepted dual, one 60-second diagnostic checks spin-resolved
components of the same ten spatial density patterns. It constructs all four
symmetry blocks, each at most 63 generators, and tests the most negative
numerically proposed direction with exact rational arithmetic. This adds
no energy solve. A negative exact anticommutator expectation identifies a
specific missing necessary constraint; it does not guarantee a bound gain.
