# Reconstruction-preserving proof compression

Preserve the sealed correlated_pair_20260913 source/results, its exact MPS
states and full-cubic lower proofs, corrected H8 convention, and CH2 model
result. New work lives only in this campaign. Replay functions that write old
receipts must not be called. No publishing, provisioning, or purchases.

The accepted MPS energy U is frozen. Required lower is exactly U−0.0016 Ha.
Track numerical coefficient matching, rationalization, and residual allowance
against that exact endpoint. No new MPS optimization on H6/H8.

Approach A: one teacher-assisted reduced-Gram diagnostic, then bases proposed
from Hamiltonian and MPS moments only. PSD variables retain complete CAR
coefficient matching and the sector-multiplier freedoms. Numerical moments
guide maps only; exact factor expansion accepts the proof. Rank/trace coverage
and state annihilation alone do not accept a result.

Approach B: fixed maps built across orbital cuts, with independent proof-map
dimensions. All compressed positive matrices arise from actual CAR operator
congruences; shared global coefficient rows retain overlap consistency and
the full molecular Hamiltonian. Never constrain unknown moments to MPS values.
Compare global low-action maps, hierarchical maps, and simple maps at similar
optimization size. Describe precisely how the finite construction differs
from published renormalization hierarchies.

Supporting verifier: certify a frozen rounded state's norm and a stated upper
endpoint inequality using rigorous enclosures. Equality may need exact
fallback. Any representational or endpoint allowance is explicit. Retain the
old exact integer checker as oracle and test refusal of invalid endpoints.

All numerical discovery, construction, tests, replay, package installation,
and failed attempts use this campaign's budget module with unique names and
explicit time caps. Do not redirect output into runner JSON files. Default
per-attempt cap180s, initial aggregate2400 completed process-wall seconds;
check the ledger before extensions. One BLAS/Numba thread and target peak
single-job RAM<=1GiB. Keep H8 in development throughout. No full determinant
Hamiltonian or FCI trial import into candidate discovery. Tiny diagnostic
oracles are explicitly separated. Record inherited costs and missing costs.

Freeze a selected rule before one genuinely fresh H6 geometry;1.91A and all
prior spacings are now development data. Complete integrated lower+frozen
upper comparisons and a transfer result even if targets fail. A bounded
search failure is not an impossibility theorem. No scalar-rescaling, response
order, or exact two-orbital parent campaign in this pass.
