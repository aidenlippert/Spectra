# Trace-normalized pricing against the frozen coefficient-ranked control

Start from the same accepted 64-combination spin-completion certificate and
numerical dual. Keep H6, its ten spatial Hamiltonian patterns, collective
tail, 492-generator frame, quadratic baseline, number ideal, solve settings,
coordinate rounding, exact exporter and accepting checker unchanged.

The frozen control is the preceding coefficient-ranked continuation in
results/spin_enrichment_20260913/campaign/continue. Its 360-second budget,
twelve-round limit, 256-combination cap, two tested bundle sizes, 60-second
reserve and exact-gain-per-cost continuation rule are retained. The new
trace-ranked run has its own 360-second outer watchdog and stops at the
first accepted 1.6 mHa interval. Do not re-solve the unchanged historical
control merely to count its discovery as new work. Preserve and verify its
manifest; recheck its best bound in fresh exact replay.

For every full frame Phi, construct T_ij = tau({Phi_i^dagger,Phi_j}), where
tau is the exact uniform fixed-N trace functional. The cached CAR map and
exact trace moments supply floating T for numerical proposals. Charge its
construction to the current budget. Refuse a numerically non-positive or
nonfinite metric; do not truncate directions or add a ridge silently.

Price the current dual with the generalized eigenproblem G_y v = lambda T v.
Propose up to two new independent directions per block with lambda < -10^-6.
Normalize each proposed vector to Euclidean length one before rounding at
10^10, preserving its direction. The negative threshold is now in trace-
normalized units; record both normalized and ordinary moments. For numerical
stability, build the pricing Grams in the orthonormal operator-coefficient
basis Q from C=QR, then map directions back through R. Use invertible
diagonal equilibration for the small generalized eigensolve. These are
pricing-only coordinate changes; no direction is dropped and the energy
solver's conditioning is unchanged. Retain the
previous independence test in operator coefficient space. All final gains
still come from exact molecular lower replay, not these eigenvalues.

Both candidate bundles and all unselected work count. Preserve the inherited
lower, strongest sampled lower and first sampled sufficient subspace under
the existing selection gates. Compare the two rules by best accepted bounds
at common elapsed-time and combination-count ceilings, without interpolating
unrun results. Report this as one historical, budget-matched comparison, not
a randomized benchmark or scaling claim. No separate-weight ablation or
full-family SDP is added in this pass.

Fresh standard-library replay covers every new accepted bound, the inherited
64-direction bound, and the frozen control's best bound against the same
rational upper. Earlier control checkpoints retain their preceding exact
replays and hashes. Prior causal descriptor discovery costs remain 222.510846501
seconds for each continuation; the failed full controls remain in earlier
whole-pass ledgers.

If the target is missed, one conditional diagnostic may reuse the preceding
300-second full-dual repair protocol and checker on the best trace-ranked
trial. It runs no full-family SDP. A physical error-floor claim requires an
accepted exact full-family witness and fresh replay of the independent
precise physical proof. Failed or weak repairs remain inconclusive.
