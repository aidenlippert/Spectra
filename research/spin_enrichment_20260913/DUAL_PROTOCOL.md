# Conditional collective check of all unselected directions

If the bounded continuation misses 1.6 mHa, use its best accepted trial's
numerical dual to attempt an exact full-family witness under a separate
300-second outer watchdog. This does not run a full-family SDP. The question
is whether the final moment assignment can be repaired so that every
direction in all eight completed-spin frames is collectively positive.

Use the existing exact Hermitian/number-ideal affine projection of moments
rounded at denominator 10^12. Construct the exact uniform fixed-N trace
functional. For each full-frame and quadratic Gram pair, floating generalized
eigenvalues propose the necessary trace mixture. They are not a proof. Test
mixtures 1.02, 1.1, 1.5 and 2 times the proposed threshold, rounded upward to
a multiple of 10^-12, with a minimum of 10^-8 and maximum of 0.01. Every
attempt and refusal is retained; no unrecorded retry follows this schedule.

Acceptance checks input binding, normalization, coefficient bounds,
Hermiticity, all body-one number identities, all quadratic PSD conditions,
and all eight complete spin-frame PSD matrices using exact rational
arithmetic. The old spin-summed gates may be reused as redundant subspace
checks. Acceptance must explicitly reconstruct all 492 generators using
the same frame builder as discovery. No numerical truncation is allowed.

An accepted y gives a ceiling y(H10)+ell for every certificate in this
completed-spin family, including every selected subspace. A true energy
error-floor claim additionally needs fresh replay of the previous precise
physical lower and its spectral residual proof for exactly the same H6
Hamiltonian. That independent audit is separate and uses no numerical
library. If the repair fails or the ceiling is too weak, report that outcome
without claiming family insufficiency.
