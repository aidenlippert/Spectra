# Fixed target and accounting

Same fully coupled open2x4 Hubbard model, U/t=8, Nup=Ndown=4, as the preceding
local-response pass. Keep the total-width target 0.001t. No molecular Hartree
accuracy or thermodynamic result is implied.

Use spin-reflection positivity to retain the full excitation information in a
matrix-space cone certificate. The exact checker must prove C positive definite
and L(C)-ell C positive semidefinite, reconstructing the full map from the
physical coefficients. An elementary variational proof must establish the PSD
ground eigenmatrix; it is not an assumed numerical property.

Cold discovery starts from the identity, with no saved ground state or endpoint.
The matrix-space solve still contains 4,900 amplitude entries. This approach
is authorized as an additional attempt to reach the numerical target, with
that dependency counted explicitly. It cannot be represented as the requested
general enumeration-free compression breakthrough. A later compact realization
would be a separate achievement.

One bounded heavy process at a time. No paid compute, installations, commits,
pushes, or changes to previous sealed artifacts. Preserve rejection paths;
use independent signed particle-hole regression and fresh standard-library
exact replay before accepting the result.
