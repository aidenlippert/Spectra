# Molecular collective completion

Preserve every earlier sealed artifact. The objective remains a complete
1.6 mHa interval for the supplied H6 and corrected H8 Hamiltonians, with a
smaller independently constructible lower proof. No numerical success on a
different spin or Hubbard model substitutes for this objective.

Investigate (1) adjoint-paired cubic positivity with all reduced cross terms,
adapted by coefficient-dual violations; (2) an exact spin-balanced-sector
identity lifted to the whole fixed-particle sector by SU(2), charging the
original Hamiltonian's exact spin defect; (3) the supplied spectral-filter
and amplitude-ratio ideas, including their missing molecular requirements.

Initial development reuses frozen Hamiltonian/MPS coefficient preparation;
its recorded cost remains a dependency. No previous successful Gram factor
or determinant-state teacher enters new discovery. Freeze a selected rule
before generating a new geometry. Existing geometries through 2.07 Angstrom
are development data.

Use the existing local environments and durable process receipts. Initial
solves are capped at 180 solver seconds and 420 process seconds; later
extensions must have a concrete measured justification and new receipts.
At most one heavy molecular solve runs at a time. No external compute or
publication. Exact rational replay alone accepts endpoints. Numerical dual
violations guide enlargement; they are not exact family obstructions.

Report all failed and successful attempts, construction dependencies,
memory, replay, and end-to-end intervals. A finite successful campaign is
not a general complexity or accuracy theorem.

## Measured extensions during this pass

H8 singlet screening and dual enrichment were given up to 300 solver seconds
after exact intervals improved from 16.31 to 7.56, then 4.86 and 2.875 mHa.
The largest attempts retain a 480-second outer process limit. Extending the
operator dictionary to global pure triples addresses terms absent from the
two-fragment dictionary. Two such larger primal formulations reached that
outer limit without producing candidates; both failures remain in the ledger.
Spin sharing lowered the representation count but did not cure that timeout.

The next numerical comparison uses the equivalent moment dual, with the same
SOS span and the same final rational reconstruction. Its purpose is to reduce
the optimizer's explicit variable count and memory. Changing SDP orientation
does not improve the mathematical relaxation by itself. It is first checked
against an independently diagonalized two-dimensional example and a cold
direct H6 construction. Further accuracy changes must come from actual span
completion or an independently certified upper, not from solver status.

## Later bounded diagnostics

After the large SDP factorizations failed within their process budgets, the
retained-response cone was tested with SCS for 240 solver seconds and a
360-second outer limit. Its numerical residual still required exact repair.
The coherent-square representation reduced the projected coefficient map by
more than an order of magnitude in the seeded comparison. This justified a
separate cold test: eight solves, each limited to 40 solver seconds, with
one 420-second outer process limit. The cold trial used the MPS quartic
guide and no previous lower-proof checkpoint. All costs and failed launches
remain in the ledger. No additional external compute was used.

The completed pass establishes the stated H6 numerical milestone and its
fresh-geometry transfer. H8 outcomes must be read from the exact receipts.
A numerical plateau, a time limit, or an omitted number-multiplier family
is not an exact impossibility certificate.
