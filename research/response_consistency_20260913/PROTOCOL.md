# Two mathematical attacks after freezing the fitted branch

Preserve the preceding 665-file mechanism-transfer manifest. Do not run
another contraction fit or adaptive H6 enrichment. Begin with explicit
identities, then diagnose their power and limitations before implementing
any energy optimization.

Attack A starts from the block-response completion of squares supplied by
the user. Investigate exact closure under elimination for a collective
charge/spin response, and test the closure assumptions against the existing
H6 spatial patterns. A compact formula is not evidence of cheap evaluation;
record where scalar closure, a gap certificate or operator-product growth
is required. Small enumerated controls must be labelled and charged.

Attack B asks whether three-particle removal positivity rejects the accepted
completed-family witness. For arbitrary C=sum c_ijk a_i a_j a_k, the operator
P=C^dagger C+C C^dagger is positive and its degree-six terms cancel exactly.
This is established T1-type positivity, not a claimed new identity. It lies
outside a mere recombination of the old charge-one generators. Whether it
actually separates the parent cone is decided by exact witness evaluation,
not generator counting.

Inspect three- through six-spatial-orbital supports in increasing size.
Use numerical eigenvectors only to propose coefficients; try small rational
roundings and verify the resulting quartic polynomial exactly. Check that
every evaluated moment belongs to the parent's degree-at-most-four domain.
The parent's sparse encoding assigns omitted moments in that domain zero;
no degree-six or other unspecified higher moments may be supplied.

If a separator is found, bind it to the unchanged parent witness and freshly
replay the full-family audit. Report the valid parametric positivity class,
the concrete exact counterexample, and what remains unproved about energy
gain, transfer, novelty and scaling. Do not build a generic proof framework.
Test any promising concrete mechanism on fresh cases, retaining all failed
attempts and charging any explicit state enumeration.

Independent molecular transfer gate, frozen after the original separator and
before new fixture generation: straight H6 at 1.6 Angstrom, STO-3G canonical
orbitals, rational export as before, ten retained patterns. Generate integrals
and an HF control only; do not run FCI. Perform exactly one solve in the full
492-generator old family with a 45-second solver cap, solely to generate a
new diagnostic moment functional. Affine-round it, propose a trace mixture
from full-frame generalized eigenvalues, and check every old rule exactly
in a fresh standard-library process. Refuse a required mixture above 1%.
Use an outer 360-second cap for discovery and 300 seconds for exact replay.
Test both the frozen twelve coefficients and the parametric T1 law, searching
three- then four-spatial supports with the same rounding schedule. Do not add
any T1 directions to an energy solve or run adaptive enrichment. Include the
fixed-family solve and every verification cost in the transfer ledger.
