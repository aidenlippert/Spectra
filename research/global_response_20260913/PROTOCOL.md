# Global response and complete collective certification

Continue the actual composable_response_20260913 source and exact artifacts.
Preserve all prior files. The accepted H6 1.000 mHa interval, 1.6 Å H6
1.400 mHa transfer, two elimination programs and CH2 model certificates
remain achieved results. No publication, remote compute use, provisioning,
or purchases are authorized in this campaign.

## Acceptance objective

Seek a complete lower proof constructed and checked without enumerating
fixed-N determinants, with substantially less actual operator execution
than the existing 12,667-H-action K2 application. The response, partition and
terminal rule may change together. Prefer <=1.6 mHa full widths with a
separately identified upper; do not disguise an inherited FCI upper as
compact discovery. Keep exact rational arithmetic on accepting paths.

## Diagnosis and independent approaches

First establish the sign of the actual K2−eta2 condition including eta1.
A stronger existing global certificate may diagnose it through the exact
Schur lower-bound lemma; any such inherited proof dependencies remain
charged and separate from new construction. Bounded enumerated numerical
spectra may characterize exceptional directions but are diagnostic only.

Compare the unchanged nested program, algebraically equivalent flattened
execution, and a simultaneous response on the combined eliminated sector.
Count Hamiltonian calls per RHS, intermediate vector support/dimensions,
non-H work, bit/float precision, memory and time. No batching shortcut may
reduce a counted set of RHS columns to one action.

Independent branches investigate: jointly chosen positive operator factors;
compact exceptional subspace with complement/coupling proof; collective
interference domination; and a compact response-lifted upper with exact norm.
An independent read-only critic checks sectors, metrics, residuals and
unreported enumeration. Existing cubic SOS, signed and wave2 work must be
considered before claiming novelty or repeating a failed family.

## Resources and transfer

All numerical BLAS work uses one thread. Initial branch budgets are 180 s
(240 s for the factor search), <=512 MiB each (factor branch <=1 GiB).
The root controls diagnostic and integration stages and can extend a bounded
branch for a concrete promising result. Overall initial numerical budget is
30 minutes of recorded local stage time; exact checking, tests and repeated
profiles are charged separately. This is a resource cap, not a success
criterion. No dense H8 full determinant matrix. Any H8 enumeration for
bounded diagnostics must be explicit and memory-capped.

Use H6 at 1.4 Å for discovery, existing H6 at 1.6 Å as a frozen validation
case, H8 as a simultaneous diagnostic. Freeze a selected rule in a saved
record before generating a new H6 geometry. The initially considered 1.8 Å
already occurs in older workspace work; before testing the selected rule,
choose 1.73 Å as the new mechanism test. Generate no H10 unless measured
costs justify it. The CH2
extension is only a model-transfer comparison against its accepted spin
certificates.

Finish with integrated exact replay, terminal-proof attempts, execution
comparison and transfer results. A positive sector lemma alone is not the
finish condition. If the complete compact construction remains unmet,
identify the actual restricted mathematical or computational obstruction
and preserve all verified advances with exact scope.
