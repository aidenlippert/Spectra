# Acceptance and selected-channel continuation

This campaign continues the sealed interacting-scaling work. All original
Hamiltonians, uppers, nonsinglet witnesses, maps, failures and successful H8/H10
milestones are preserved. There are no GitHub operations or external spending.

The initial comparison uses the same H12 checkpoint for a 300-second continuation
of the existing schedule and a 300-second fixed-scalar feasibility construction.
The latter targets b = U - 1 mHa. Both retain the full original Gram family.
Their outcomes are numerical proposals until the original complete checker runs.
The first control timed out after library/file-loading delays; it remains charged.
The warmed-file repeat is separately recorded, with its unchanged 300-second
numerical budget and a 480-second process cap.

Direct coefficient-L1 fitting is a distinct restricted experiment: first fix all
Gram matrices and optimize the admitted ideal coefficients and scalar; then allow
nonnegative rescaling of the existing whole Gram blocks. Each LP has a 180-second
limit. A numerical optimum in this restricted subcone is not a full-family
obstruction. The scalar, residual and complete predicted width are reported.

The pasted H8 separator is reconstructed from its four integer coefficients.
An independent occupation-action oracle checks its polynomial identity on its
seven involved modes. The original exact dual was not attached. Its reported
negative value cannot be replayed from the prose. Explicit rational coordinates
and an exact spin-average identity establish membership of the supplied formula
interpreted in the current H8 and H12 orbital coordinates. The physical source
basis match or required transport cannot be established from this formula alone.

The next conditional selector therefore uses the new H12 control checkpoint.
For each beta spectator mode it constructs a 132-by-132 anticommutator moment
matrix from available degree-at-most-four moments. It inspects the twelve lowest
eigenvectors and refits their four largest coordinates. Duplicate word sets and
sets already contained in one retained highest-weight block are discarded.
At most eight remaining four-word sets are retained by numerical violation.
The source dual is approximate; no exact exclusion theorem is claimed.

Each selected set adds a complete 4-by-4 PSD block, shared between adjoint factors.
This permits joint coefficients and internal cross terms. Exact quartic
cancellation is checked before constructing its map, and every required row must
already exist. No full global cubic map or unspecified higher moment is used.
The eight-block ceiling is 128 extra Gram entries. The search has a 180-second
process cap; the extended solver and an unchanged-family control each receive
300 seconds, starting from the identical control checkpoint. All selection,
construction and replay costs are additional to earlier discovery.

Only useful candidates receive a full new original-model replay (900-second
process cap per replay). The exact standard-library checker remains authoritative.
This is a warm continuation and mechanism test, not a new cold calculation or
an end-to-end advantage over the optimized molecular reference.

The numerical FCI control from the earlier sealed campaign is read only to
diagnose the upper's scale of error. It suggests about 0.037 mHa of upper-state
error, but is not a rigorous enclosure, a proposal state, or an optimization input.

Results must distinguish a target-passing exact certificate, an exact obstruction,
and an undecided bounded search. A residual plateau alone is not an obstruction.

After the common-spectator search returned no selected violation, a separately
declared diagnostic expands to the complete spin-three-half paired T2 moment
matrix (792 operators on H12, hard cap 1,024). Its entries use an explicit quartic
anticommutation identity and existing moments. This larger dense operator matrix
is charged discovery work; it is not a matrix-free or scalable selector claim.
The four-word/eight-block selection and subsequent acceptance rules stay fixed.

The global diagnostic found a numerical eigenvalue near -0.001914, while the
four-word truncations retained no selected violation. A second, separately
recorded support test therefore allows 4, 8, 16, 32 and 64 words, taking the first
negative refitted submatrix for each inspected vector. At most four blocks are
retained for the next molecular solve. This is at most 16,384 additional Gram
entries. The size increase is a charged diagnostic change, not a frozen-transfer
success. Exact quartic identities and accepting equations remain unchanged.

The 4--64-word tests also retained no selected violation. The following declared
test retains up to four complete negative eigenvectors of the 792-operator
matrix instead. Each direction may have all 792 coefficients. A single coupled
4-by-4 Gram block adds at most 16 entries. This is a count of collective
coordinates, not a claim of four short polynomials. The adjoint pairing cancels
sextic terms exactly. Three quartic contractions construct the projected maps;
a Woodbury normal update avoids materializing their dense outer product.
The unchanged original checker still re-expands the exported factors.

This dense-direction extension and a further unchanged-family control each
receive 300 solver seconds and a 480-second process cap, starting from the exact
same warmed-control checkpoint. The comparison is conditional on that shared
history, and all the earlier unsuccessful attempts remain additional.

A separately bounded numerical control uses the installed SCS algorithm on the
unchanged equality-constrained SDP, starting from the same warmed-control
checkpoint. It receives 180 solver seconds and a 480-second process cap. This
tests a different numerical method; it is not the full accepted-L1 epigraph
formulation and an inaccurate SCS status is not a family obstruction. Its PSD
proposal is scored by the original residual rule, retained only if better, and
requires the same complete exact replay as all other proposals.

The full accepted-L1 formulation is a separate SCS test on that identical source
checkpoint and unchanged Gram family, with 180 solver seconds and a 480-second
process cap. It includes every Gram variable, all independent ideals and the
actual lifted residual norm. The conic dual uses y=K^T d with |d|<=w; no residual
coefficient is dropped. Both the SCS ideal fit and a least-squares refit are
scored after PSD projection, and the source is retained if both are worse.
Small real conic solves first verify the formulation on a case with a necessary
nonzero remainder and on a full 2-by-2 matrix. Numerical status alone is not an
acceptance result or an exact family limitation.

After the four-direction matched test, permit up to two further batches of four
collective directions (at most twelve in total, 144 Gram entries). Each selector
uses the latest extended checkpoint and searches the orthogonal complement of
the already retained directions. The previous rational basis columns and its
complete Gram block are embedded unchanged before optimization, so the family
is nested. Each continuation receives 300 solver seconds and a 480-second process
cap. This adaptive branch has additional history and is not a new matched-time
comparison or a frozen transfer result. Its source, selected coefficients,
construction cost and unsuccessful attempts remain explicit dependencies.

The first nested selector suffered a filesystem timeout while reading an
offloaded checkpoint; its retry passed. The first eight-direction constructor
was stopped after 180 seconds with 2.17 CPU seconds, while loading the maps.
Materialize identical inputs into a local cache outside the synced Documents
tree, checking all prepared files against the sealed dependency hashes, then
retry the same numerical experiment. Cache population has a 300-second process
cap and is charged setup work. This storage workaround is not an algorithmic
speedup and does not change the accepted model or any coefficient map.

The eight-direction continuation did not improve the retained four-direction
best under its budget. A direct polynomial-versus-map evaluation on the saved
molecular dual agrees within 3e-15, so that test found no map discrepancy.
Use the final allowed batch to inspect the spin-change-one paired T2 channel,
rather than another batch in spin change three. This builds a declared
2,520-by-2,520 moment matrix (cap 3,000) from supplied quadratic and quartic
moments and retains at most four directions. The existing eight-direction
block is preserved separately; the total is at most twelve directions and
80 added Gram entries. Selection has a 180-second process cap and solving has
300 numerical seconds / 480 process seconds. This larger diagnostic matrix
and each up-to-2,520-term direction count as discovery and proof dependencies.
Exact CAR and exact small singlet-trace tests must pass before this run.

A 90-second numerical singlet-trace diagnostic may inspect forced null spaces
and the cost of trace mixing on the unchanged family. It cannot establish an
exact obstruction without rational affine/nullspace repair and accepted PSD
checks. Its output explicitly distinguishes these missing proof steps.

The first trace diagnostic timed out during loading after 90 seconds and only
0.32 CPU seconds; it produced no mathematical result. An optional byte-copy
cache of installed SciPy 1.18.1 likewise timed out after 300 seconds. That
partial runtime was not added to any import path. Both failed stages remain
charged. The complete focused regression suite subsequently passed using the
original installed runtime. Exact replay uses the original standard-library
checker and does not import SciPy.

Coordinate scope of the supplied H8 polynomial: the exact membership check
uses its formula verbatim in the current orbital labelling. No match to the
external witness's physical orbital basis was established without its bundle.
Transporting an operator from a different basis may change membership. The
H12 searches and certificates are independently bound to their current frame.

The first complete four-direction replay exhausted its 900-second cap, after
743.5 user CPU seconds, without producing a complete lower receipt. Its partial
files and entire cost remain. Extend the unchanged standard-library replay
envelope to 1,800 seconds in a separate run and folder; no accuracy condition or
accepting arithmetic changes. This is necessary to establish whether the
numerical candidate is a certificate, and directly counts against its cost.

The accepting-cost prototype recognizes only an exactly matched adjacent pair
of spin-transfer and adjoint dictionaries with identical integer factors.
It contracts that pair directly to its quartic polynomial using arbitrary-size
Python integers. Every original word, charge, denominator and factor validation
is retained; all unmatched blocks use the original expansion. Five exact tests
compare polynomials and statistics with the original implementation, including
large integers, duplicates, reversed annihilators and malformed inputs.

After the original full molecular replay finishes, a 900-second complete replay
may exercise this standard-library contraction path. Its exact lower, residual,
and original-model interval must equal the original replay before any accepting
cost improvement is claimed. This addresses only the paired additions, not the
full global bookkeeping or H12 accuracy target.

The exact contraction tests also compare all 256 occupation-state columns on an
eight-mode oracle using literal ladder action, independently of normal-order
polynomial multiplication. A separately timed component run evaluates only
the strictly matched paired part of the actual H12 proposal; it cannot claim
a full energy interval or a speedup over an untimed reference component.

The extended original replay passed with width 2.588188188369 mHa. A separate
original-checker replay of the unchanged-family matched control uses the same
1,800-second process envelope. Comparing saved numerical logs also reveals a
limitation of the equal-time comparison: the four-direction run completed
1,408 iterations versus 1,201. At every common positive logged iteration its
predicted width was worse. The final equal-time result must not be described
as establishing that the new correlations caused the improvement.

The matched control passed original replay at 2.821202290321 mHa. The single
allowed 90-second trace-diagnostic retry also completed, using the original
installed numerical runtime. It found a nonzero trace-kernel defect; its
positive-face-only mixture would weaken the proposed dual energy by about
1.194 mHa. It supplies no exact family obstruction. The final bounded full
paired-contraction replay uses the previously declared 900-second process cap.

The trace retry isolated 12 null directions in each 216-dimensional highest-spin
block. A small exact-algebra diagnostic identifies the operators S_plus a_p_beta
and S_plus a_p_alpha_dagger in those two actual dictionaries, verifies their
commutators and coordinate independence, and checks an independent two-site
singlet oracle. This uses no molecular solve and does not repair a dual. Its
process cap is 60 seconds; it may run alongside replay as a light algebra check.

If the complete paired-contraction replay matches the original receipt, time the
same paired component through the original CAR expansion with a 180-second cap.
Compare its complete exact polynomial and factor statistics with the previously
timed integer contraction. This isolates the component from the observed
throughput variation in numerical solves; it does not turn one replay timing
into a reproducible complete-pipeline speedup. Run it after replay, not alongside.
