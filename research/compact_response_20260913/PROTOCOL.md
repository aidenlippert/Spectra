**A compact molecular response, with a full-bound comparison**

Preserve the preceding 744-file manifest and the prior 430-byte H6 sector
certificate. The main partition remains Q=n_(5,up)n_(5,down), P=I-Q.
The main comparison uses the frozen physical upper and the 3.415421065516425
mHa accepted interval. The 1.6 mHa target and the exact old-family floor
1.5134646589803447 mHa are separate thresholds. Improving an upper witness
must be reported separately from improving the lower at the fixed upper.

First construct an operator-program response, not a stored many-body inverse.
Given independently proved delta I <= D=Q(H-b)Q <= M I, define
c=(M+delta)/2, a=(M-delta)/2, Z=(cI-D)/a, z0=c/a. For k>=1,
r_k(D)=T_k(Z)/T_k(z0), p_(k-1)(D)=(I-r_k(D))/D is a polynomial, with
||I-Dp(D)||<=1/T_k(z0). Implement p through its divided-difference recurrence,
so the definition performs no inverse and stores no expanded operator powers.
For X=p(D)B*, E=B*-DX obeys ||E||<=||B||/T_k(z0). Therefore

 K=A-BX-X*B*+X*DX >= eta I,
 eta=||B||^2/[delta*T_k(z0)^2],

is sufficient for H>=b. Derive and exact-check the spectral and coupling
bounds from the Hamiltonian and projectors. Charge recurrence length,
coefficient size and every action required by any use of the program.

The response-error certificate is only one part of a full lower proof.
Attempt the retained-remainder certificate explicitly. If a cheap closure
fails, distinguish that failure from response approximation error. Small
enumerated diagnostics may expose the missing work but cannot be counted
as a scalable response or an independently compact full calculation. Any
remaining solve and every generated or acted-on determinant must be charged.
Do not replace the primary result with a new fit in the old 492-generator
family, or silently attribute a changed upper witness to a better lower.

As a physical trial control, evaluate the same response on the retained part
of the frozen upper witness. Any improved trial is rounded to an explicit
integer witness and replayed exactly. Inherited FCI discovery remains charged.
This can improve a complete two-sided interval even if the fixed-upper lower
comparison does not improve; those outcomes must occupy separate columns.

Freeze the response construction rule before checking one existing held-out
fixture. Use the same endpoint construction and stopping criterion, without
fitting new response coefficients. The initial recurrence cap is 256 and the
desired certified residual penalty is 1e-6 Ha. Cap any direct small control at
the existing H6 sector; no H8 full-sector matrix or inverse is allowed.

Transfer accounting: H8 was examined during endpoint-rule development, so it
is a development control. The response-rule holdout is the existing H6 1.6 A
fixture from response_consistency_20260913, with its single-determinant upper.
The same last-orbital projector, b=U-0.001 Ha, endpoint rounding, recurrence
cap and residual budget apply. This fixture existed in earlier investigations;
"held out" refers to the response rule, not to the project as a whole. The
physical trial optimizes only a two-dimensional Rayleigh quotient after the
response direction is fixed, then replays the rounded integer vector exactly.

The supporting physical-validation track is a small primary-source pilot
using existing measurements, with observables and model errors defined before
claiming predictions. It does not authorize a new broad chemistry campaign.

All accepting checks use exact rational arithmetic and Python -S. Numerical
response actions or retained spectra are proposals/diagnostics only. Record
failed closures and exhausted budgets, preserve the best accepted interval,
and state whether any numerical mechanism milestone was actually reached.
